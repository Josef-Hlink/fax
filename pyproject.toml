[project]
name = "fax"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "91252200+Josef-Hlink@users.noreply.github.com" }
]
requires-python = ">=3.11"
dependencies = [
    "attrs>=25.3.0",
    "distrax>=0.1.5",
    "flax>=0.10.6",
    "gymnasium>=1.1.1",
    "jax[cuda12]>=0.6.0",
    "loguru>=0.7.3",
    "melee",
    "ruff>=0.11.8",
    "tensordict>=0.8.2",
    "torch>=2.0.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
melee = { git = "https://github.com/vladfi1/libmelee.git", rev = "dev" }
